{
  Vector entry;
  Iterator itr;
  entry=(Vector)mTestDefinitions.get(shortForm);
  if (entry == null) {
    return false;
  }
  itr=entry.iterator();
  while (itr.hasNext()) {
    if (itr.next().toString().equalsIgnoreCase(longForm)) {
      return true;
    }
  }
  return false;
}
