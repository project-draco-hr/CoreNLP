{
  StringBuffer result=new StringBuffer();
  for (int i=0; i < s.length(); i++) {
    char c=s.charAt(i);
    if (c == '_') {
      c=' ';
    }
    result.append(c);
  }
  return result.toString();
}
