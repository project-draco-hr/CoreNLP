{
  List<Tree> verbMatches=runPattern(t,verbMatcher);
  System.err.printf("%s: Found %d verbs\n",this.getClass().getName(),verbMatches.size());
  if (VERBOSE)   printTreesToDebug(verbMatches);
  return verbMatches;
}
