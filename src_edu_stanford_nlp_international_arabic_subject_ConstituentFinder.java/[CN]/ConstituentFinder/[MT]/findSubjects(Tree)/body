{
  List<Tree> subjMatches=runPattern(t,subjMatcher);
  System.err.printf("%s: Found %d subjects\n",this.getClass().getName(),subjMatches.size());
  if (VERBOSE)   printTreesToDebug(subjMatches);
  return subjMatches;
}
