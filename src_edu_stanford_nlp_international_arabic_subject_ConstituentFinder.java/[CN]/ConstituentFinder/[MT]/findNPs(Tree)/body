{
  List<Tree> npMatches=runPattern(t,npMatcher);
  System.err.printf("%s: Found %d NPs\n",this.getClass().getName(),npMatches.size());
  if (VERBOSE)   printTreesToDebug(npMatches);
  return npMatches;
}
