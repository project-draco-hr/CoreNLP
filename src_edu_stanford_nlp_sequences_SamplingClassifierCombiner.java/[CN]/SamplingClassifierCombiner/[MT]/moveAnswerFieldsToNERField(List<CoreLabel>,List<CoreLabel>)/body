{
  if (sent.size() != ner.size()) {
    throw new RuntimeException("Size Mismatch!");
  }
  Iterator<CoreLabel> nerIter=ner.iterator();
  for (  CoreLabel m : sent) {
    m.setNER(nerIter.next().get(AnswerAnnotation.class));
    m.set(AnswerAnnotation.class,null);
  }
}
