{
  InvertedIndexByTokens inv=new InvertedIndexByTokens(props,stopWords,specialWords,constVars);
  if (sentences != null && sentences.size() > 0)   inv.add(sentences);
  System.out.println("Created index with size " + inv.size());
  return inv;
}
