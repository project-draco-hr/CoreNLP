{
  if (filebacked)   index=new FileBackedCache<String,Hashtable<String,Set<String>>>(invertedIndexDir);
 else   index=new HashMap<String,Hashtable<String,Set<String>>>();
  this.convertToLowercase=lc;
  this.stopWords=stopWords;
  if (this.stopWords == null)   this.stopWords=new HashSet<String>();
  this.stopWords.add("FW");
  this.stopWords.add("SW");
  this.stopWords.add("fw");
  this.stopWords.add("sw");
}
