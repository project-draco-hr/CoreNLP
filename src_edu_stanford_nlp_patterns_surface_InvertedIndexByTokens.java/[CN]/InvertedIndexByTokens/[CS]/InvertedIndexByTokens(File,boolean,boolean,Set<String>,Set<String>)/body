{
  if (filebacked)   index=new FileBackedCache<StringwithConsistentHashCode,Hashtable<String,Set<String>>>(invertedIndexDir,10);
 else   index=new HashMap<StringwithConsistentHashCode,Hashtable<String,Set<String>>>();
  this.convertToLowercase=lc;
  this.stopWords=stopWords;
  if (this.stopWords == null)   this.stopWords=new HashSet<String>();
  this.specialWords=specialWords;
}
