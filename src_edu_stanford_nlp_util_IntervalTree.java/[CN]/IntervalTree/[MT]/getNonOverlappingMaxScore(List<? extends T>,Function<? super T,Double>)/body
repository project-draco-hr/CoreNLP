{
  Function<T,Interval<E>> toIntervalFunc=new Function<T,Interval<E>>(){
    @Override public Interval<E> apply(    T in){
      return in.getInterval();
    }
  }
;
  return getNonOverlappingMaxScore(items,toIntervalFunc,scoreFunc);
}
