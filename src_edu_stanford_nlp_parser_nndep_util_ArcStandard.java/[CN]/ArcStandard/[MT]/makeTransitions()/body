{
  transitions=new ArrayList<String>();
  for (int i=0; i < labels.size(); ++i)   transitions.add("L(" + labels.get(i) + ")");
  for (int i=0; i < labels.size(); ++i)   transitions.add("R(" + labels.get(i) + ")");
  transitions.add("S");
}
