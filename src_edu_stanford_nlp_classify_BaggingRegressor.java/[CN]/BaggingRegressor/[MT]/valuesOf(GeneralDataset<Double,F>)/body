{
  List<Double> avgValues=new ArrayList<Double>();
  for (int i=0; i < data.size(); i++)   avgValues.add(0.0);
  for (  Regressor<F> regressor : bagOfRegressors) {
    List<Double> values=regressor.valuesOf(data);
    for (int i=0; i < values.size(); i++)     avgValues.set(i,avgValues.get(i) + values.get(i));
  }
  for (int i=0; i < avgValues.size(); i++)   avgValues.set(i,avgValues.get(i) / bagOfRegressors.size());
  return avgValues;
}
