{
  if (mByteOffset != null) {
    return new String("['" + WORDS.get(mWord) + "', "+ OTHERS.get(mPos)+ ", "+ mByteOffset.start()+ ", "+ mByteOffset.end()+ "]");
  }
  return new String("['" + WORDS.get(mWord) + "', "+ OTHERS.get(mPos)+ "]");
}
