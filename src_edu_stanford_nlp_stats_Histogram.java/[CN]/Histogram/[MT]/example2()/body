{
  int trials=5000;
  double p=0.5;
  double n=5000;
  double[] samples=new double[trials];
  for (int trial=0; trial < trials; trial++)   for (int i=0; i < n; i++)   if (Math.random() < p)   samples[trial]++;
  Histogram h=new Histogram(samples,20);
  System.out.println(h.toString(0,true));
}
