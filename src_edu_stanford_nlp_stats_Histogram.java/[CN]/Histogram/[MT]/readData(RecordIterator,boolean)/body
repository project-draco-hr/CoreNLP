{
  DataSeries[] serieses=ListDataSeries.readDataSeries(it,useHeaders);
  if (serieses == null || serieses.length == 0)   return null;
  DataSeries series=serieses[0];
  double[] v=new double[series.size()];
  for (int i=0; i < v.length; i++) {
    v[i]=series.get(i);
  }
  return v;
}
