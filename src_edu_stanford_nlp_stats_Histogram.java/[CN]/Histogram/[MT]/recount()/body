{
  ClassicCounter<Integer> bucketCounts=new ClassicCounter<Integer>();
  X=ArrayMath.filterNaN(X);
  for (int i=0; i < X.length; i++)   bucketCounts.incrementCount(bucketFor(X[i]));
  for (int i=0; i < buckets + 2; i++)   counts[i]=(int)bucketCounts.getCount(i);
}
