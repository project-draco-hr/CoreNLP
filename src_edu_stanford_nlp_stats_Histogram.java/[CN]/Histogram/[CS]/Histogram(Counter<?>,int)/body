{
  double[] x=new double[X.size()];
  int i=0;
  for (  double t : X.values()) {
    x[i]=t;
    i++;
  }
  setDataNoRecount(x);
  setBucketsNoRecount(buckets);
  setRangeNoRecount();
  recount();
}
