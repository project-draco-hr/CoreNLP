{
  try {
    PennTreeReader ptr=new PennTreeReader(new StringReader(treeString),treeFactory,treeNormalizer);
    return ptr.readTree();
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
