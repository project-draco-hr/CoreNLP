{
  String line=inputFields[FIELD_INDEX_INPUT] + '\t' + datum.label()+ '\t'+ answer+ '\t'+ nf.format(scoreDist.probabilityOf(answer))+ '\t'+ nf.format(scoreDist.probabilityOf(datum.label()));
  System.out.println(line);
}
