{
  String[] words=mwe.split("_");
  for (int i=0; i < words.length - 1; i++) {
    String word=words[i].toLowerCase();
    if (word.equals("de")) {
      String next=words[i + 1].toLowerCase();
      if (!dictionary.containsKey(next))       return positiveRet;
    }
  }
  return negativeRet;
}
