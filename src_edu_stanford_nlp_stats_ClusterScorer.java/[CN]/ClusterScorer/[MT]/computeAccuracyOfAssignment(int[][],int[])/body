{
  int numTotal=0;
  int numCorrect=0;
  for (int j=0; j < confusionMatrix[0].length; j++) {
    for (int i=0; i < confusionMatrix.length; i++) {
      numTotal+=confusionMatrix[i][j];
    }
    numCorrect+=confusionMatrix[assignment[j]][j];
  }
  return (double)numCorrect / (double)numTotal;
}
