{
  try {
    return prob(new DateRangePair(wsdate,false),new DateRangePair(camdate,false),new DateRangePair(notedate,false),new DateRangePair(subdate,false));
  }
 catch (  ParseException e) {
    return 0.0;
  }
catch (  NumberFormatException e) {
    return 0.0;
  }
}
