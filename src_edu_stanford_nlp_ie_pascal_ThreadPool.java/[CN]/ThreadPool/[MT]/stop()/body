{
  for (int i=0; i < threads.length; ++i) {
    threads[i].safeStop();
  }
  for (int i=0; i < threads.length; ++i) {
    boolean joined=false;
    while (!joined) {
      try {
        threads[i].join();
        joined=true;
      }
 catch (      InterruptedException e) {
      }
    }
  }
}
