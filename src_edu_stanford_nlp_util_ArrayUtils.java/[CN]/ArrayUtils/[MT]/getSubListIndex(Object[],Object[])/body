{
  return getSubListIndex(tofind,tokens,new Function<Pair,Boolean>(){
    @Override public Boolean apply(    Pair objectObjectPair){
      return objectObjectPair.first().equals(objectObjectPair.second());
    }
  }
);
}
