{
  return new Penalty(){
    @Override public double derivative(    double w){
      return alpha * w;
    }
    @Override public String toString(){
      return String.format("l2(%s)",alpha);
    }
  }
;
}
