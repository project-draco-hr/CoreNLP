{
  ClassicCounter<Object> scores=new ClassicCounter<Object>();
  String line=getLine(example);
  for (int i=0; i < labelIndex.size(); i++) {
    scores.setCount(labelIndex.get(i),getScore(line,i + 1));
  }
  return (scores);
}
