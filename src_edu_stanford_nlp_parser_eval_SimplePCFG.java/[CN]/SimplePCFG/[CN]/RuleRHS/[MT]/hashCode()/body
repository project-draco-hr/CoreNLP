{
  if (m_hashcode == -1) {
    StringBuilder sb=new StringBuilder();
    for (int i=0; i < m_rhs.length; i++)     sb.append(m_rhs[i]);
    m_hashcode=sb.toString().hashCode();
  }
  return m_hashcode;
}
