{
  int lastSplit=0;
  int outI=0;
  for (int i=0; i < input.length(); ++i) {
    if (input.charAt(i) == delimiter) {
      out[outI]=input.substring(lastSplit,i);
      outI+=1;
      lastSplit=i + 1;
    }
  }
}
