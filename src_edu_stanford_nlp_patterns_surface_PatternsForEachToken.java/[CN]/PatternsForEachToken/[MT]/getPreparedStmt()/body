{
  return conn.prepareStatement("UPDATE table SET patterns=? WHERE sentid=? and tokenid=?; " + "INSERT INTO " + tableName + "(sentid, tokenid, patterns) + VALUES (?,?,?) WHERE NOT EXISTS (SELECT 1 FROM table WHERE sentid=? and tokenid=?;");
}
