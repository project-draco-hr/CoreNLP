{
  if (tag == null || tag.equals("")) {
    return;
  }
 else   if (tag.equals("!--")) {
    discardCommentTags=false;
    return;
  }
  String temp=tag.toLowerCase();
  keeperTags.put(temp,temp);
}
