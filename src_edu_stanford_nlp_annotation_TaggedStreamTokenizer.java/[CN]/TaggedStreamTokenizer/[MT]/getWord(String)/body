{
  if (tagInScope) {
    attr=currentTag;
    sval=s;
    ttype=TT_TARGET_WORD;
    return TT_TARGET_WORD;
  }
 else {
    attr=null;
    sval=s;
    ttype=TT_BACKGROUND_WORD;
    return TT_BACKGROUND_WORD;
  }
}
