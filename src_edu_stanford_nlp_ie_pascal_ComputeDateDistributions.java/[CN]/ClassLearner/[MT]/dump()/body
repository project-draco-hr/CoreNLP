{
  StringBuffer sb=new StringBuffer();
  if (probs != null) {
    for (int i=0; i < probs.length; ++i) {
      sb.append(probs[i]);
      if (i < probs.length - 1) {
        sb.append(",");
      }
    }
  }
  return sb.toString();
}
