{
  double total=0;
  for (  Map.Entry<E,Double> entry : doc.entrySet()) {
    total+=entry.getValue();
  }
  for (  Map.Entry<E,Double> entry : doc.entrySet()) {
    entry.setValue(entry.getValue() / total);
  }
}
