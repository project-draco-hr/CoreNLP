{
  if (monitor != null) {
    monitor.apply(x);
  }
  SortedSet<Integer> toBeAssigned=new TreeSet<Integer>();
  for (  int i : batch) {
    toBeAssigned.add(i);
  }
  doComputation(toBeAssigned,x);
}
