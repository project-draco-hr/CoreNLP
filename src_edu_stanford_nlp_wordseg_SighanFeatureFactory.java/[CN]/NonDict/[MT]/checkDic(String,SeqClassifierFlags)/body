{
  CorpusDictionary nd;
  if (flags.useAs) {
    nd=as;
  }
 else   if (flags.useHk) {
    nd=hk;
  }
 else   if (flags.useMsr) {
    nd=msr;
  }
 else   if (flags.usePk) {
    nd=pk;
  }
 else {
    nd=ctb;
  }
  if (nd.getW(c2).equals("1"))   return "1";
  return "0";
}
