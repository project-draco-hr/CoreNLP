{
  int blockRow=0, blockCol=0;
  if (row > topRows) {
    blockRow=1;
    row-=topRows;
  }
  if (col > leftCols) {
    blockCol=1;
    col-=leftCols;
  }
  m[blockRow][blockCol].set(row,col,value);
}
