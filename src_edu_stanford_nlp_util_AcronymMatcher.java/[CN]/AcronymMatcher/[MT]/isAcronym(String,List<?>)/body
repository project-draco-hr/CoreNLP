{
  List<String> strs=new ArrayList<String>(tokens.size());
  for (  Object tok : tokens) {
    strs.add(tok instanceof String ? tok.toString() : ((CoreLabel)tok).word());
  }
  return isAcronymImpl(str,strs);
}
