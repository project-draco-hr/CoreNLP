{
  if (datum instanceof RVFDatum)   return valueOfRVFDatum((RVFDatum<Double,F>)datum);
  double output=0;
  for (  F feature : datum.asFeatures()) {
    int fID=featureIndex.indexOf(feature);
    if (fID >= 0)     output+=weights[fID];
  }
  return output;
}
