{
  double output=0;
  Counter<F> fCounter=datum.asFeaturesCounter();
  for (  F feature : fCounter.keySet()) {
    int fID=featureIndex.indexOf(feature);
    if (fID >= 0)     output+=weights[fID] * fCounter.getCount(feature);
  }
  return output;
}
