{
  double value=0.0;
  int rank=0;
  double found=0.0;
  for (  T item : items) {
    ++rank;
    if (isRelevant.apply(item)) {
      ++found;
      value+=found / rank;
    }
  }
  double size=totalRelevant == null ? found : totalRelevant;
  return value / size;
}
