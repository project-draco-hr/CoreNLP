{
  char[] cutChars=tlp.labelAnnotationIntroducingCharacters();
  for (  char cutChar : cutChars) {
    if (ch == cutChar) {
      return true;
    }
  }
  if (ch == '-')   return true;
  return false;
}
