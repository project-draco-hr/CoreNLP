{
  ShiftReduceParser parser=IOUtils.readObjectAnnouncingTimingFromURLOrClasspathOrFileSystem("Loading parser from serialized file",path);
  if (extraFlags.length > 0) {
    parser.setOptionFlags(extraFlags);
  }
  return parser;
}
