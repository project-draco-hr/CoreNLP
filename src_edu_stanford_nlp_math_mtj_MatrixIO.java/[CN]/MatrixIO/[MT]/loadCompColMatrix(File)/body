{
  MatrixVectorReader reader=new MatrixVectorReader(new InputStreamReader(IOUtils.openFile(file)));
  try {
    return new CompColMatrix(reader);
  }
 catch (  IOException e) {
    throw new IOUtils.QuietIOException(e);
  }
}
