{
  String stripped=pair.first().toLowerCase();
  String firstPron=pair.second().get(0).toLowerCase();
  return (isVerb(stripped) || firstPron.matches("os") && isVerb(stripped + 'd') || firstPron.matches("nos|se") && isVerb(stripped + 's'));
}
