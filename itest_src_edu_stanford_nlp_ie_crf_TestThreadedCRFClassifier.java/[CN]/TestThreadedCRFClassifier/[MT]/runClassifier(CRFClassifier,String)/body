{
  try {
    ByteArrayOutputStream output=new ByteArrayOutputStream();
    crf.classifyAndWriteAnswers(testFile,output,crf.makeReaderAndWriter());
    return output.toString(inputEncoding);
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
}
