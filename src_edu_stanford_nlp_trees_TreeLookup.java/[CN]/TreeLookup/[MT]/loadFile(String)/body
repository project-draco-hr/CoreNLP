{
  FileFilter filter=new FileFilter(){
    public boolean accept(    File file){
      return file.getName().equals(filename) || file.isDirectory();
    }
  }
;
  FilePathProcessor.processPath(treebankDir,filter,this);
}
