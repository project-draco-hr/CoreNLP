{
  Label tempLabel=tree.label();
  String s=tempLabel.toString();
  if (s == null) {
    return 0;
  }
  int numVerbs=0;
  for (int i=0; i < s.length(); i++) {
    if (s.charAt(i) == '_') {
      numVerbs++;
    }
  }
  return numVerbs;
}
