{
  DenseLU lu=DenseLU.factorize(v);
  double det=1.0;
  Matrix l=lu.getL();
  for (int i=0; i < l.numRows(); ++i) {
    det*=l.get(i,i);
  }
  Matrix u=lu.getU();
  for (int i=0; i < u.numRows(); ++i) {
    det*=u.get(i,i);
  }
  return det;
}
