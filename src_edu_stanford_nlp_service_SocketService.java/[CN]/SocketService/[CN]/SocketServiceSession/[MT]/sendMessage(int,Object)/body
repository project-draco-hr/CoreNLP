{
  try {
    out.reset();
    out.writeInt(rid);
    out.writeUnshared(message);
    out.flush();
  }
 catch (  NotSerializableException e) {
    e.printStackTrace();
    System.exit(1);
  }
catch (  IOException e) {
    close();
    return false;
  }
  return true;
}
