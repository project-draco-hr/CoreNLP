{
  if (str.equals("BIES_COLLAPSED"))   return BIES_COLLAPSED;
 else   if (str.equals("ORIGINAL"))   return ORIGINAL;
 else   throw new IllegalArgumentException("Don't know anything about tagset " + str);
}
