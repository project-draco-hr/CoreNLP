{
  double[] source=null;
  int key=0;
  for (  Map.Entry<Integer,double[]> entry : toBeCombined.entrySet()) {
    key=entry.getKey();
    source=entry.getValue();
    for (int i=0; i < source.length; i++)     combineInto[key][i]+=source[i] * scale;
  }
}
