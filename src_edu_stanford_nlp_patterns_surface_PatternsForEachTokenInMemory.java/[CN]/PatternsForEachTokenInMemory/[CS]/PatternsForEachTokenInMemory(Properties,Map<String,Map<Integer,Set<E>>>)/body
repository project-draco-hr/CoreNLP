{
  Execution.fillOptions(this,props);
  if (patternsForEachToken == null)   patternsForEachToken=new ConcurrentHashMap<String,Map<Integer,Set<? extends Pattern>>>();
  if (pats != null)   addPatterns(pats);
}
