{
  Execution.fillOptions(this,props);
  if (patternsForEachToken == null)   patternsForEachToken=new ConcurrentHashMap<String,Map<Integer,Set<Integer>>>();
  if (pats != null)   addPatterns(pats);
}
