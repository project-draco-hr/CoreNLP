{
  String s="X";
  if (useTag)   s+=":" + tag;
  if (useNER)   s+=":" + nerTag;
  if (useTargetParserParentRestriction)   s+=":" + grandparentParseTag;
  if (numWordsCompound > 1)   s+="{" + numWordsCompound + "}";
  return s;
}
