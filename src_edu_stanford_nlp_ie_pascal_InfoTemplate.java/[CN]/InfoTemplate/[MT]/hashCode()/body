{
  int tally=31;
  int n=7;
  tally=whomepage.hashCode() + n * wacronym.hashCode() + n * n * wname.hashCode();
  tally+=(chomepage.hashCode() + n * cacronym.hashCode() + n * n * cname.hashCode());
  return tally;
}
