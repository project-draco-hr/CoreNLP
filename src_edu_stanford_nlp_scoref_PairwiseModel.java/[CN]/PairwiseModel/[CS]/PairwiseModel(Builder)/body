{
  name=builder.name;
  meta=builder.meta;
  trainingExamples=builder.trainingExamples;
  epochs=builder.epochs;
  singletonRatio=builder.singletonRatio;
  classifier=new SimpleLinearClassifier(builder.loss,builder.penalty,builder.learningRateSchedule,builder.modelFile == null ? null : (builder.modelFile.endsWith("model.ser") ? builder.modelFile : StatisticalCorefTrainer.pairwiseModelsPath + builder.modelFile + "/model.ser"));
  str=StatisticalCorefUtils.fieldValues(builder);
}
