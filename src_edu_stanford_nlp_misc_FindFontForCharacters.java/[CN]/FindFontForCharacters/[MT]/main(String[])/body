{
  if (args.length > 0) {
    for (    String arg : args) {
      System.out.println("Fonts that can display " + arg + " are "+ findFontForCharacters(arg));
    }
  }
 else {
    System.out.println("Fonts that can display Chinese are " + findFontForChinese());
  }
}
