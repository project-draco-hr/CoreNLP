{
  if (type != null && type.indexOf("TP") != -1) {
    return TP;
  }
  if ("FP".equals(type)) {
    return FP;
  }
  if ("FN".equals(type)) {
    return FN;
  }
  throw (new IllegalArgumentException());
}
