{
  StringWriter buf=new StringWriter();
  buf.write("Ssurgeon Wordlist Resource, id=");
  buf.write(id);
  buf.write(", elements=(");
  for (  String word : words) {
    buf.write(" ");
    buf.write(word);
  }
  buf.write(")");
  return buf.toString();
}
