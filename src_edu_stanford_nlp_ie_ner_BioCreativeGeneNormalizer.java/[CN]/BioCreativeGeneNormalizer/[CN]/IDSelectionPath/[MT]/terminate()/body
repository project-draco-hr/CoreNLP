{
  if (score > maxScore) {
    maxScore=score;
    best=new ArrayList<ArrayList<String>>();
    best.add(new ArrayList<String>(ids));
  }
 else   if (score == maxScore) {
    best.add(new ArrayList<String>(ids));
  }
}
