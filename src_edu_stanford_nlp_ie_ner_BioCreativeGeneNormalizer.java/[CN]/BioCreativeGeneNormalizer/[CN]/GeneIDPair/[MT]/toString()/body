{
  StringBuffer sb=new StringBuffer();
  sb.append(gene);
  sb.append(':');
  for (Iterator<String> iter=ids.iterator(); iter.hasNext(); ) {
    sb.append(iter.next());
    sb.append(' ');
  }
  return sb.toString();
}
