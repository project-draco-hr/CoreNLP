{
  try {
    editorPane.setPage(url);
  }
 catch (  Exception e) {
    e.printStackTrace();
    displayError("Error Loading URL " + url,"Message: " + e.toString());
    return;
  }
  loadedFile=null;
  redraw();
}
