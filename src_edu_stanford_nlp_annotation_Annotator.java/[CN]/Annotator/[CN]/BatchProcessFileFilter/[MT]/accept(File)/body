{
  if (file.isFile()) {
    if (unannotatedOnly) {
      File targetFile=new File(targetDir + File.separator + file.getName());
      if (targetFile.exists()) {
        return false;
      }
    }
    return true;
  }
  return false;
}
