{
  StringBuilder buff=new StringBuilder();
  for (int i=0; i < s.length(); i++) {
    char c=s.charAt(i);
    if (c == '^') {
      buff.append('\\');
    }
    buff.append(c);
  }
  return buff.toString();
}
