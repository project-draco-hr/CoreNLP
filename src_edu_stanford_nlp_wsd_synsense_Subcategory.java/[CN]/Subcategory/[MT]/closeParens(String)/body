{
  int num=0;
  for (int i=0; i < s.length(); i++) {
    if (s.charAt(i) == '(') {
      num++;
    }
 else     if (s.charAt(i) == ')') {
      num--;
    }
  }
  for (int i=0; i < num; i++) {
    s=s.concat(")");
  }
  return s;
}
