{
  List<CoreLabel> tokens=AnnotationUtils.sentenceEntityMentionsToCoreLabels(sentence,true,null,mentionTypes,false,useBIO);
  for (  CoreLabel t : tokens) {
    os.printf("%s %s %s\n",t.word(),t.tag(),t.get(AnswerAnnotation.class));
  }
}
