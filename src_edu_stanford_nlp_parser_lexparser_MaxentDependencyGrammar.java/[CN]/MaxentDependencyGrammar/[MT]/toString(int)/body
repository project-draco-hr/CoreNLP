{
  StringBuffer sb=new StringBuffer();
  sb.append(intFromBits(bits,21,31) - 1);
  sb.append("|" + (intFromBits(bits,10,20) - 1));
  sb.append("|" + (intFromBits(bits,4,9)));
  sb.append("|" + intFromBits(bits,0,3));
  return sb.toString();
}
