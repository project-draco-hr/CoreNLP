{
  if (args.length < 2) {
    System.err.println(usage);
    System.exit(1);
  }
  HamleDTCorrector corrector=new HamleDTCorrector(args[0],args[1]);
  List<String> corrected=corrector.correct();
  for (  String line : corrected)   System.out.println(line);
}
