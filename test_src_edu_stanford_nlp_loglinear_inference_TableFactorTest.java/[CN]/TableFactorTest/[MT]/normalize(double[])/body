{
  double sum=0;
  for (  double d : arr)   sum+=d;
  if (sum == 0) {
    for (int i=0; i < arr.length; i++) {
      arr[i]=1.0 / arr.length;
    }
  }
 else {
    for (int i=0; i < arr.length; i++) {
      arr[i]=arr[i] / sum;
    }
  }
}
