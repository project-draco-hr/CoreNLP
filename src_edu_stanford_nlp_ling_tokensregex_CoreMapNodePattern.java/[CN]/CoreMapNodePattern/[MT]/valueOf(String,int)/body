{
  CoreMapNodePattern p=new CoreMapNodePattern(new ArrayList<Pair<Class,NodePattern>>(1));
  p.add(CoreAnnotations.TextAnnotation.class,newStringRegexPattern(textAnnotationPattern,flags));
  return p;
}
