{
  if (entityLabel.contains("ORG"))   return 0;
 else   if (entityLabel.contains("GPE"))   return 1;
 else   if (entityLabel.contains("PERSON"))   return 2;
 else   if (entityLabel.contains("LOC"))   return 3;
 else   return 4;
}
