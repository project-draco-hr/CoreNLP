{
  boolean caught=false;
  try {
    new MergedMatrix(new DenseMatrix(new double[][]{{1,2},{0,1}}),new DenseMatrix(new double[][]{{0,0},{1,1}}));
  }
 catch (  Exception e) {
    caught=true;
  }
  assertTrue("Didn't throw exception on invalid sparsity",caught);
}
