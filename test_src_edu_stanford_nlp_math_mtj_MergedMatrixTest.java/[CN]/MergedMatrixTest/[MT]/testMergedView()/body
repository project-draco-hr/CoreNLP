{
  MergedMatrix merged=new MergedMatrix(new DenseMatrix(new double[][]{{1,0,3},{0,5,0}}),new DenseMatrix(new double[][]{{0,-2},{-3,0}}));
  assertTrue("Matrix not equal",equals(merged,new DenseMatrix(new double[][]{{1,-2,3},{-3,5,0}})));
  double[][] mergedArray=new double[2][3];
  for (int row=0; row < mergedArray.length; row++) {
    for (int col=0; col < mergedArray[row].length; col++) {
      mergedArray[row][col]=merged.get(row,col);
    }
  }
  assertTrue("Matrix not equal",equals(new DenseMatrix(mergedArray),new DenseMatrix(new double[][]{{1,-2,3},{-3,5,0}})));
}
