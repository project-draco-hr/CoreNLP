{
  for (int i=0; i < typeSeq.length; i++) {
    System.err.print(" ");
    if (numTypes >= 10 && typeSeq[i] < 10) {
      System.err.print(" ");
    }
    System.err.print(typeSeq[i]);
  }
  System.err.println();
}
