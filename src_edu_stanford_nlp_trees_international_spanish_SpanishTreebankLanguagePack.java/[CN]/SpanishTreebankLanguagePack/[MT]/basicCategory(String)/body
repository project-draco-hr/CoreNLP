{
  if (category == null)   return null;
  if (pPOSTag.matcher(category).find()) {
    if (category.charAt(0) == 'v')     return category.substring(0,2);
 else     return category.substring(0,1);
  }
  return category;
}
