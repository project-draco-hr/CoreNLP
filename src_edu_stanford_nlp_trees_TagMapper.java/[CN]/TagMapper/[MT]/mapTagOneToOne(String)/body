{
  if (l == null) {
    return null;
  }
 else   if (containsKey(l)) {
    String[] s=tagMap.get(l);
    return s[0];
  }
 else {
    throw new RuntimeException("No mapping present for tag " + l);
  }
}
