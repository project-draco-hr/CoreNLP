{
  if (in == null) {
    return (null);
  }
  BufferedReader br=getBufferedReader(in);
  StringBuilder sb=new StringBuilder(16000);
  int c;
  while ((c=br.read()) >= 0) {
    sb.append((char)c);
  }
  return sb.toString();
}
