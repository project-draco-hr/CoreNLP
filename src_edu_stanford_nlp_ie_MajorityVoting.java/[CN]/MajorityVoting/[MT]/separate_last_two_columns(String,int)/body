{
  s=s.trim();
  String retVal[]=new String[3];
  retVal[0]="";
  retVal[1]="";
  retVal[2]="";
  StringTokenizer st=new StringTokenizer(s);
  if (st.countTokens() < columns) {
    retVal[0]=s;
    return retVal;
  }
  for (int i=0; i < columns - 2; i++)   retVal[0]+=st.nextToken() + " ";
  retVal[1]=st.nextToken();
  retVal[2]=st.nextToken();
  return retVal;
}
