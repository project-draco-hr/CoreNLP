{
  double b=getBeta(k);
  double numer=n.getCount(parentK,k) + b * alpha;
  double denom=n.totalCount(parentK) + alpha;
  if (verbose > 3)   System.err.println("trans " + parentK + " to "+ k+ ": b="+ b+ " alpha="+ alpha+ " count="+ n.getCount(parentK,k)+ " total="+ n.totalCount(parentK)+ " numer="+ numer+ " denom="+ denom);
  double result=(numer / denom);
  return result;
}
