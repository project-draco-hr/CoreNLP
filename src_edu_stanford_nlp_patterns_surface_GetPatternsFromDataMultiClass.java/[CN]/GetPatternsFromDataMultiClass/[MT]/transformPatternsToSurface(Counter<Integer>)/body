{
  return Counters.transform(pats,new Function<Integer,SurfacePattern>(){
    @Override public SurfacePattern apply(    Integer integer){
      return constVars.getPatternIndex().get(integer);
    }
  }
);
}
