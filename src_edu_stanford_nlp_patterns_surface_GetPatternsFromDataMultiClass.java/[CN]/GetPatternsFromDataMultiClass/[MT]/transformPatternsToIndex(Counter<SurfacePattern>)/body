{
  return Counters.transform(pats,new Function<SurfacePattern,Integer>(){
    @Override public Integer apply(    SurfacePattern pat){
      return constVars.getPatternIndex().indexOf(pat);
    }
  }
);
}
