{
  int tp=0, fp=0;
  Feature f=functions.get(fNo);
  int yNo=f.getY(0);
  for (int l=0; l < f.len(); l++) {
    int x=f.getX(l);
    int cls=train.getClass(x);
    if (cls == yNo) {
      tp++;
    }
 else {
      fp++;
    }
  }
  return tp / (double)(tp + fp);
}
