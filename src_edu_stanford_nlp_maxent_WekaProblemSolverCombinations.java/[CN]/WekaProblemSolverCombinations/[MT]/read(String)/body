{
  try {
    train=new ReadDataWeka(filename);
    mE=new MaxEntModel();
    mE.read(filename + ".me");
    model=mE.prob;
    if (!clean) {
      printFeatures();
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
}
