{
  double[] posters=getPosteriorsNom(d);
  double max=Double.NEGATIVE_INFINITY;
  int maxI=-1;
  for (int z=0; z < posters.length; z++) {
    if (posters[z] > max) {
      max=posters[z];
      maxI=z;
    }
  }
  return maxI;
}
