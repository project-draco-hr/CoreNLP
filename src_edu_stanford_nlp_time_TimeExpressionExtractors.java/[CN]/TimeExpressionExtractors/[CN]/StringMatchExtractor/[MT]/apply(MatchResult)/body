{
  TimeExpression te=new TimeExpression(Interval.toInterval(matched.start(group),matched.end(group),Interval.INTERVAL_OPEN_END),null,extractor,0,0);
  te.setIncludeNested(includeNested);
  return te;
}
