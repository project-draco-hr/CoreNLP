{
  if (!tuned) {
    throw (new IllegalStateException("must call tuneParameters before calling get"));
  }
  if (wordModel) {
    return (Math.exp(pnpc.getLogProb(s)));
  }
 else {
    return (getCharProb(s));
  }
}
