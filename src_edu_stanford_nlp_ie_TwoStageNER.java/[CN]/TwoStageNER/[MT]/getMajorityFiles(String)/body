{
  String cmd="paste ";
  for (int i=1; i <= 6; i++) {
    String majFile=resultsFile + "_maj" + i;
    CommandLineCall.execute(getJavaCmd() + " edu.stanford.nlp.ie.NamedEntityConfusionMatrices " + resultsFile+ " "+ i+ " > "+ majFile);
    cmd+=majFile + " ";
  }
  cmd+=" > " + resultsFile + "_majorities";
  CommandLineCall.execute(cmd);
}
