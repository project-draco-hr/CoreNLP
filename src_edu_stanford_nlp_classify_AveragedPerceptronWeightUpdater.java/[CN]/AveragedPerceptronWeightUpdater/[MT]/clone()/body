{
  AveragedPerceptronWeightUpdater<F,ID> rtn=new AveragedPerceptronWeightUpdater<F,ID>();
  rtn.infoMap=new HashMap<F,Info>();
  for (  Map.Entry<F,Info> e : infoMap.entrySet()) {
    rtn.infoMap.put(e.getKey(),e.getValue().clone());
  }
  rtn.time=this.time;
  return rtn;
}
