{
  int i=1;
  int leng=cat.length();
  for (; (i < leng); i++) {
    if (tlp.isLabelAnnotationIntroducingCharacter(cat.charAt(i))) {
      break;
    }
  }
  if (i < leng) {
    return newBaseCat + cat.substring(i);
  }
 else {
    return newBaseCat;
  }
}
