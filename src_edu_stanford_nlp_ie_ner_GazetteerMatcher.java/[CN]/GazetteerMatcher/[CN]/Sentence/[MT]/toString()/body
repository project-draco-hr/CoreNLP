{
  StringBuilder s=new StringBuilder();
  Iterator i=iterator();
  while (i.hasNext()) {
    s.append(i.next());
    if (i.hasNext()) {
      s.append("\n");
    }
  }
  s.append("\n");
  return s.toString();
}
