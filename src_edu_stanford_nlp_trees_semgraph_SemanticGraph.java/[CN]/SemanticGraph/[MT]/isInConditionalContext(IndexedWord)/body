{
  List<IndexedWord> children=this.getChildrenWithReln(vertex,EnglishGrammaticalRelations.MARKER);
  for (  IndexedWord child : children) {
    if (child.word().equalsIgnoreCase("if"))     return true;
  }
  return false;
}
