{
  if (!hasConfidence()) {
    return false;
  }
  if (!hasSubjectSpan()) {
    return false;
  }
  if (!hasRelationSpan()) {
    return false;
  }
  if (!hasObjectSpan()) {
    return false;
  }
  if (!getSubjectSpan().isInitialized()) {
    return false;
  }
  if (!getRelationSpan().isInitialized()) {
    return false;
  }
  if (!getObjectSpan().isInitialized()) {
    return false;
  }
  if (hasTree()) {
    if (!getTree().isInitialized()) {
      return false;
    }
  }
  return true;
}
