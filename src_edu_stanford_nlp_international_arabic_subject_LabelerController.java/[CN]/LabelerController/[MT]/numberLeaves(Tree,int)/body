{
  if (parseTree.isLeaf()) {
    parseTree.setScore((double)startIndex);
    return startIndex + 1;
  }
 else {
    for (    Tree child : parseTree.children())     startIndex=numberLeaves(child,startIndex);
    return startIndex;
  }
}
