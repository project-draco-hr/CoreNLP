{
  for (int i=0; i < posTags.size(); i++) {
    String thisLabel=posTags.get(i);
    if (thisLabel.matches("NN.*|NP.*"))     posTags.set(i,"NN");
 else     if (thisLabel.matches("VB.*"))     posTags.set(i,"VB");
  }
}
