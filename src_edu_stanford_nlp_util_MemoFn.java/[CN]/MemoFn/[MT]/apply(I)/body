{
  O output;
  if (memos.containsKey(input)) {
    output=memos.get(input);
    if (announceMemos)     report("memo",input,output);
  }
 else {
    output=compute(input);
    putMemo(input,output);
    if (announceComputes)     report("compute",input,output);
  }
  return output;
}
