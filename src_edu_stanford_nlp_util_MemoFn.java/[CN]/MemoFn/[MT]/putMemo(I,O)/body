{
  boolean changed=false;
  if (memos.containsKey(input)) {
    O old=memos.get(input);
    if (old == null ? output == null : old.equals(output)) {
      changed=false;
    }
 else {
      changed=true;
    }
  }
 else {
    changed=true;
  }
  isChanged|=changed;
  memos.put(input,output);
  return changed;
}
