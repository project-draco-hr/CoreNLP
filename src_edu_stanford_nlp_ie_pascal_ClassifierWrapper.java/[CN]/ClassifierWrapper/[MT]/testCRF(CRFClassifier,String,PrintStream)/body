{
  ObjectBank<List<CoreLabel>> docs=crf.makeObjectBankFromFile(testFile,crf.makeReaderAndWriter());
  for (  List<CoreLabel> doc : docs) {
    crf.classify(doc);
    crf.printProbsDocument(doc);
  }
}
