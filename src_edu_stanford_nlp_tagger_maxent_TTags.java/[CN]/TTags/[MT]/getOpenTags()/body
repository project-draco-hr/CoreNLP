{
  if (openTags == null) {
    HashSet<String> open=new HashSet<String>();
    for (    String tag : index) {
      if (!closed.contains(tag)) {
        open.add(tag);
      }
    }
    openTags=open;
  }
  return openTags;
}
