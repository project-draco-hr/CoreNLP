{
  pw.println("Answer\tGoldAnswer\tCharacter");
  for (  CoreLabel word : doc) {
    pw.printf("%s\t%s\t%s%n",word.get(AnswerAnnotation.class),word.get(GoldAnswerAnnotation.class),word.get(CharAnnotation.class));
  }
}
