{
  List<HasWord> ans=new ArrayList<HasWord>(arg);
  for (  HasWord wd : ans) {
    String w=wd.word();
    String newW=StringUtils.tr(w,"()","\uFF08\uFF09");
    wd.setWord(newW);
  }
  return ans;
}
