{
  int totalSize=totalParamSize();
  if (TRAIN_WORD_VECTORS) {
    return RNNUtils.paramsToVector(totalSize,binaryTransform.valueIterator(),unaryTransform.values().iterator(),binaryScore.valueIterator(),unaryScore.values().iterator(),wordVectors.values().iterator());
  }
 else {
    return RNNUtils.paramsToVector(totalSize,binaryTransform.valueIterator(),unaryTransform.values().iterator(),binaryScore.valueIterator(),unaryScore.values().iterator());
  }
}
