{
  double sum=0;
  double temp;
  Iterator<Double> iter=rankings.values().iterator();
  while (iter.hasNext()) {
    if ((temp=(iter.next()).doubleValue()) != -1) {
      sum+=temp;
    }
  }
  return sum;
}
