{
  int[] types=new int[size()];
  Iterator<Word> iter=iterator();
  int i=0;
  while (iter.hasNext()) {
    Word w=iter.next();
    if (w instanceof HasType) {
      types[i]=((HasType)w).type();
    }
 else {
      types[i]=0;
    }
    i++;
  }
  return (types);
}
