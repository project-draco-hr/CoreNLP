{
  if (row >= getRowCount()) {
    return (null);
  }
  String slotName=(String)slotNames[row];
switch (col) {
case 0:
    return slotName;
case 1:
  return valuesBySlot.get(slotName);
default :
return (null);
}
}
