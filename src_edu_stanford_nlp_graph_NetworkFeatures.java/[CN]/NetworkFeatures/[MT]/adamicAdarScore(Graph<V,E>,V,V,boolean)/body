{
  if (!directionSensitive)   throw new Exception("not implemented");
  double score=0;
  for (  V v : CollectionUtils.intersection(graph.getChildren(node1),graph.getParents(node2))) {
    score+=-Math.log(graph.getOutDegree(v));
  }
  return score;
}
