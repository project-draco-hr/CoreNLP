{
  boolean adjustCharacterOffsets=false;
  if (srcSentence == null || srcSentence.get(TextAnnotation.class) == null || srcTokens.size() == 0 || srcTokens.get(0).get(OriginalTextAnnotation.class) == null) {
    adjustCharacterOffsets=true;
  }
  return copyTokens(srcTokens,adjustCharacterOffsets,true);
}
