{
  double w=-x;
  double w_prime, diff;
  for (int i=0; i < 100; i++) {
    w_prime=-x - Math.log(Math.abs(w));
    diff=Math.abs((w_prime - w) / w);
    if (diff < TOLERANCE) {
      return w_prime;
    }
    w=w_prime;
  }
  System.err.println("MAX ITER REACHED");
  return w;
}
