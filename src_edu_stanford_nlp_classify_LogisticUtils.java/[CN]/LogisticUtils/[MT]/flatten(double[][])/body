{
  int length=0;
  for (  double[] array : input)   length+=array.length;
  double[] result=new double[length];
  int count=0;
  for (  double[] array : input) {
    for (    double value : array)     result[count++]=value;
  }
  return result;
}
