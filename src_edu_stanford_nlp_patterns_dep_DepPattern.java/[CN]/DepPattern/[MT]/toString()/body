{
  if (relations.size() > 1)   throw new UnsupportedOperationException();
  Pair<Token,GrammaticalRelation> rel=relations.get(0);
  String p="(" + rel.first().toString() + "=parent >"+ rel.second().toString()+ "=reln {}=node)";
  return p;
}
