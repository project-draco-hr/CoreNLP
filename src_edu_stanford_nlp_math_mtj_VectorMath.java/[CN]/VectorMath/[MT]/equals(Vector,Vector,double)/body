{
  if (a.size() != b.size()) {
    return false;
  }
  final int n=a.size();
  for (int i=0; i < n; i++) {
    if (Math.abs(a.get(i) - b.get(i)) > tolerance) {
      return false;
    }
  }
  return true;
}
