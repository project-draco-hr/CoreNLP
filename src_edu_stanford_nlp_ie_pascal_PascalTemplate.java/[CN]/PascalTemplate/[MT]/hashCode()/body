{
  int tally=37;
  for (int i=0; i < values.length - 1; i++) {
    int n;
    if (values[i] == null) {
      n=11;
    }
 else {
      n=values[i].hashCode();
    }
    tally=17 * tally + n;
  }
  return tally;
}
