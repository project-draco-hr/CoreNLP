{
  String str="\n====================\n";
  for (int i=0; i < values.length; i++) {
    if (values[i] != null) {
      if (!(values[i].equalsIgnoreCase("NULL"))) {
        str=str.concat(fields[i] + " : " + values[i]+ "\n");
      }
    }
  }
  return str;
}
