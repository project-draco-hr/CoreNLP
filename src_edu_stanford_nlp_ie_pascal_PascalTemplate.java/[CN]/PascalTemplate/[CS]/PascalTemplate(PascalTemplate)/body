{
  this.values=new String[fields.length];
  for (int i=0; i < values.length; i++) {
    if (pt.values[i] == null) {
      this.values[i]=null;
    }
 else {
      this.values[i]=pt.values[i];
    }
  }
}
