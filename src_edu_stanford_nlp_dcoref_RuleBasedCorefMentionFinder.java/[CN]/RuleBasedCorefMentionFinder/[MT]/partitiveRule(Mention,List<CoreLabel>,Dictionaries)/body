{
  if (m.startIndex >= 2 && sent.get(m.startIndex - 1).get(TextAnnotation.class).equalsIgnoreCase("of") && dict.parts.contains(sent.get(m.startIndex - 2).get(TextAnnotation.class).toLowerCase())) {
    return true;
  }
  return false;
}
