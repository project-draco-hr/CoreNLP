{
  assertTrue(SpanishVerbStripper.isStrippable("decirme"));
  assertTrue(SpanishVerbStripper.isStrippable("decirnos"));
  assertTrue(SpanishVerbStripper.isStrippable("jugarles"));
  assertTrue(SpanishVerbStripper.isStrippable("mandarlos"));
  assertTrue(SpanishVerbStripper.isStrippable("leerlo"));
  assertTrue(SpanishVerbStripper.isStrippable("jugarla"));
  assertTrue(SpanishVerbStripper.isStrippable("jug??rselos"));
  assertTrue(SpanishVerbStripper.isStrippable("dec??rmelo"));
  assertTrue(SpanishVerbStripper.isStrippable("cont??ndolo"));
  assertTrue(SpanishVerbStripper.isStrippable("y??ndole"));
  assertTrue(SpanishVerbStripper.isStrippable("vivi??ndolo"));
  assertTrue(SpanishVerbStripper.isStrippable("ley??ndolo"));
  assertTrue(SpanishVerbStripper.isStrippable("busc??ndome"));
  assertTrue(SpanishVerbStripper.isStrippable("sentaos"));
  assertTrue(SpanishVerbStripper.isStrippable("vest??os"));
  assertTrue(SpanishVerbStripper.isStrippable("c??mprelos"));
  assertTrue(SpanishVerbStripper.isStrippable("h??zmelo"));
  assertTrue(SpanishVerbStripper.isStrippable("sent??monos"));
  assertTrue(SpanishVerbStripper.isStrippable("escrib??mosela"));
}
