{
  Counter<Integer> cc=new ClassicCounter<>();
  for (  Map.Entry<E,Double> entry : counts.entrySet()) {
    Integer count=(int)Math.round(entry.getValue());
    cc.incrementCount(count);
  }
  return cc;
}
