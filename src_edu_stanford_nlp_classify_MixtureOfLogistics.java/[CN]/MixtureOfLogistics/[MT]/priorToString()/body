{
  String priorString="Prior distribution:\n";
  for (int j=0; j < numComponents; j++)   priorString=priorString.concat(Math.exp(logMixtureWeights[j]) + " ");
  priorString=priorString.concat("\n");
  return priorString;
}
