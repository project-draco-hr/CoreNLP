{
  if (size() == 0) {
    return " ";
  }
 else   if (size() == 1) {
    return data.toString();
  }
 else {
    return previous.internalToString(delimiter) + "," + delimiter+ data.toString();
  }
}
