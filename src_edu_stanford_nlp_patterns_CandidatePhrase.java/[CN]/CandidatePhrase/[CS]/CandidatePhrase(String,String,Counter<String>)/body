{
  if (phrase.isEmpty())   new Exception("Creating empty candidatePhrase").printStackTrace(System.out);
  this.phrase=phrase;
  this.phraseLemma=lemma;
  this.features=features;
  this.hashCode=phrase.hashCode();
}
