{
  String inputFile="/u/kvijay/data/ner/column_data/eng_transformed.testb";
  String outputFile="/u/kvijay/data/ner/column_data/eng_transformed_IO.testb";
  int columns=4;
  String format="IO";
  if (args.length > 3)   columns=Integer.parseInt(args[3]);
  try {
    convertFormat(inputFile,outputFile,columns,format);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
  System.out.println("done");
}
