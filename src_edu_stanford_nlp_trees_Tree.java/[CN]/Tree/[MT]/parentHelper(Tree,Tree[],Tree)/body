{
  for (int i=0, n=kids.length; i < n; i++) {
    if (kids[i] == node) {
      return parent;
    }
    Tree ret=node.parent(kids[i]);
    if (ret != null) {
      return ret;
    }
  }
  return null;
}
