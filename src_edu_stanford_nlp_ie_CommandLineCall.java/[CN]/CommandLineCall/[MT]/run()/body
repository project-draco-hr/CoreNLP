{
  try {
    BufferedReader r=new BufferedReader(new InputStreamReader(s));
    String l;
    while ((l=r.readLine()) != null)     System.out.println(n + "> " + l);
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
