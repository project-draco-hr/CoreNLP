{
  Structure s=new Structure();
  s.giveDefault();
  s.lengthenTarget(0);
  s.lengthenTarget(0);
  s.addTarget(1);
  s.addPrefix(1);
  s.addSuffix(2);
  s.addPrefix(3);
  s.getTransitions();
  State[] struct=s.getStates();
  System.err.println(s);
}
