{
  try {
    System.err.print("connectiong ... ");
    SocketClient<B,C> service=SocketClient.connect(hostAndPort.first(),hostAndPort.second());
    System.err.print("done\nprocessing " + service + " ... ");
    System.err.println("return value: " + serviceProcessor.apply(service));
    return initInfo;
  }
 catch (  Exception e) {
    e.printStackTrace();
    return null;
  }
}
