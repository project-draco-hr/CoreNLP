{
  if (children != null) {
    for (    K key : children.keySet()) {
      List<K> p=new ArrayList<K>(prefix.size() + 1);
      p.addAll(prefix);
      p.add(key);
      children.get(key).updateKeys(keys,p);
    }
  }
  if (value != null) {
    keys.add(prefix);
  }
}
