{
  if (s == null)   return s;
  int add=defaultPadding - s.length();
  if (add <= 0)   return s;
  StringBuffer str=new StringBuffer(s);
  char[] ch=new char[add];
  Arrays.fill(ch,' ');
  str.append(ch);
  return str.toString();
}
