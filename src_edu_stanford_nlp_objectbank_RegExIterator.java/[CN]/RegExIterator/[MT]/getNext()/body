{
  try {
    boolean b=m.find();
    if (b) {
      if (m.groupCount() > 0) {
        return m.group(1);
      }
 else {
        return m.group();
      }
    }
 else {
      return null;
    }
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}
