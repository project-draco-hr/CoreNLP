{
  for (int i=0; i < originalDates.length; i++) {
    assertEquals("Testing " + relativeArguments[i] + " with respect to "+ originalDates[i],relativeDateAnswers[i],(new ISODateInstance(new ISODateInstance(originalDates[i]),relativeArguments[i])).getDateString());
  }
}
