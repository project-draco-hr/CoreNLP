{
  List<T> okayChunks=new ArrayList<T>(chunks.size());
  for (  T chunk : chunks) {
    Value v=chunk.value;
    if (v == null || v.get() == null) {
    }
 else {
      okayChunks.add(chunk);
    }
  }
  return okayChunks;
}
