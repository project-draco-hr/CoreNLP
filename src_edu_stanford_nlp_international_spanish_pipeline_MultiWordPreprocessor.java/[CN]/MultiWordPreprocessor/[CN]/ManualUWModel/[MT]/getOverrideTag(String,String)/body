{
  if (containingPhrase == null)   return null;
  if ((word.equalsIgnoreCase("este") && !containingPhrase.startsWith(word)) || (actuallyNames.contains(word)))   return "np00000";
 else   if (word.equals("ma??ana"))   return "nc0s000";
  return null;
}
