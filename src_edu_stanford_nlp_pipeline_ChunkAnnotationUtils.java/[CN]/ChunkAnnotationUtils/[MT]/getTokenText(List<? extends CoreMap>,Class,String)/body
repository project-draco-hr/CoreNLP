{
  StringBuilder sb=new StringBuilder();
  for (  CoreMap t : tokens) {
    if (sb.length() != 0) {
      sb.append(delimiter);
    }
    sb.append(t.get(tokenTextKey));
  }
  return sb.toString();
}
