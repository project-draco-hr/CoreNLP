{
  Set<IndexedWord> descendantSet=new HashSet<IndexedWord>();
  if (doNotAddThese != null && doNotAddThese.contains(vertex))   return descendantSet;
  descendantsHelper(g,vertex,descendantSet,allCutOffRels,doNotAddThese,new ArrayList<IndexedWord>(),ignoreCommonTags);
  return descendantSet;
}
