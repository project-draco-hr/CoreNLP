{
  try {
    if (auxReader == null) {
      if (datasetAuxReaderClass != null) {
        auxReader=datasetAuxReaderClass.getConstructor().newInstance();
      }
    }
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return auxReader;
}
