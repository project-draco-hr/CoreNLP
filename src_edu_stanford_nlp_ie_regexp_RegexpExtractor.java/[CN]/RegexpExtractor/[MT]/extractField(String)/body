{
  re=Pattern.compile(regexp);
  Matcher m=re.matcher(text);
  if (m.find()) {
    String matched;
    if (m.groupCount() > 0) {
      matched=m.group(1);
    }
 else {
      matched=m.group(0);
    }
    return matched;
  }
 else {
    return "";
  }
}
