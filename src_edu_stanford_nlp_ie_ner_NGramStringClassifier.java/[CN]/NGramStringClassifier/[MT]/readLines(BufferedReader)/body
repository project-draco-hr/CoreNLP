{
  List<LineInfo> l=new ArrayList<LineInfo>();
  while (in.ready()) {
    String line=in.readLine();
    LineInfo li=makeLineInfo(line);
    l.add(li);
  }
  if (useEnds) {
    endify(l);
  }
  return l;
}
