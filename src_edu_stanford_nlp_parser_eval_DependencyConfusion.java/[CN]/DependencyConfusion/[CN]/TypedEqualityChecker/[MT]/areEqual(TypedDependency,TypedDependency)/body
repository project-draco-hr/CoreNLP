{
  return td.headIndex == td1.headIndex && td.depIndex == td1.depIndex && td.headTerm.equals(td1.headTerm) && td.depTerm.equals(td1.depTerm) && td.headCat.equals(td1.headCat) && td.depCat.equals(td1.depCat);
}
