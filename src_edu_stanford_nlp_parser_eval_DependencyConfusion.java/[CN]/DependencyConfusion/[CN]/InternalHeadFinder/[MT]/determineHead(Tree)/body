{
  if (t.value().equals(SPECIAL_ROOT_SYMBOL)) {
    for (    Tree child : t.children())     if (child.value().equals(SPECIAL_HEAD_SYMBOL))     return child;
    return t.children()[0];
  }
  return hf.determineHead(t);
}
