{
  String w;
  Iterator<T> iter=sent.iterator();
  while (iter.hasNext()) {
    w=iter.next().word();
    if (w.equals("======================================") || w.equals("[") || w.equals("]")) {
      iter.remove();
    }
  }
  return sent;
}
