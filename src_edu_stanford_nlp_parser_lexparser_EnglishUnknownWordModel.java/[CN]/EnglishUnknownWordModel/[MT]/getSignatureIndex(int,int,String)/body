{
  String uwSig=getSignature(word,sentencePosition);
  int sig=wordIndex.addToIndex(uwSig);
  if (DEBUG_UWM) {
    System.err.println("Signature (" + unknownLevel + "): mapped "+ word+ " ("+ index+ ") to "+ uwSig+ " ("+ sig+ ")");
  }
  return sig;
}
