{
  if (base == 0) {
    base=0;
    exponent=0;
  }
 else {
    while (base < 1) {
      base=(base * 10);
      exponent-=1;
    }
    while (base >= 10) {
      base=(base / 10);
      exponent+=1;
    }
  }
}
