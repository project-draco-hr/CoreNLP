{
  String lastWord=m.originalSpan.get(m.originalSpan.size() - 1).word();
  return lastWord.length() > 0 && m.spanToString().endsWith("???") && countries.contains(lastWord.substring(0,lastWord.length() - 1));
}
