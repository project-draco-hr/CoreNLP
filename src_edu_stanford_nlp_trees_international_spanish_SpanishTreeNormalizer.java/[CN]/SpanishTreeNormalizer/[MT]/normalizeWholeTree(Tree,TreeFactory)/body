{
  for (  Tree t : tree) {
    if (t.isPreTerminal()) {
      CoreLabel label=(CoreLabel)t.label();
      String pos=label.value();
      pos=simplifyPOSTag(pos).intern();
      label.setValue(pos);
      label.setTag(pos);
    }
  }
  return tree;
}
