{
  objectToElement=new HashMap<T,Element>();
  for (Iterator<? extends T> i=objectSet.iterator(); i.hasNext(); ) {
    T o=i.next();
    Element e=new Element(o);
    objectToElement.put(o,e);
  }
}
