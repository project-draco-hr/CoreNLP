{
  StringWriter ret=new StringWriter();
  Iterator<String> it=datums.iterator();
  if (it.hasNext()) {
    ret.write(it.next().toString());
    while (it.hasNext()) {
      ret.write(delim);
      ret.write(it.next().toString());
    }
  }
  return ret.toString();
}
