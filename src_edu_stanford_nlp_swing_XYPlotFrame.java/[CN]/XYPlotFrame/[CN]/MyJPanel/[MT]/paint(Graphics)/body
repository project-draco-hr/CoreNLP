{
  g.setColor(Color.white);
  g.drawRect(0,0,xMax - 1,yMax - 1);
  for (int x=0; x < xMax; x++) {
    for (int y=0; y < yMax; y++) {
      if (grid[x][y] != null) {
        g.setColor(grid[x][y]);
        g.fillRect(x,y,8,8);
      }
    }
  }
}
