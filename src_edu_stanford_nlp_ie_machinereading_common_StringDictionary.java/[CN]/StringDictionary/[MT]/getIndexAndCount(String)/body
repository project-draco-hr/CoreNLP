{
  IndexAndCount ic=mDict.get(s);
  if (mCreate == true) {
    if (ic == null) {
      ic=new IndexAndCount(mDict.size(),0);
      mDict.put(s,ic);
      mInverse.put(new Integer(ic.mIndex),s);
    }
    ic.mCount++;
  }
  return ic;
}
