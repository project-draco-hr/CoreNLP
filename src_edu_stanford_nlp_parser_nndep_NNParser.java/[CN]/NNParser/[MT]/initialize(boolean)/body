{
  if (labelDict == null)   throw new IllegalStateException("Model has not been loaded or trained");
  List<String> lDict=new ArrayList<>(labelDict);
  lDict.remove(0);
  system=new ArcStandard(lDict);
  classifier.preCompute();
}
