{
  Token value=null;
  value=jj_consume_token(STR);
{
    if ("" != null)     return value.image.substring(1,value.image.length() - 1);
  }
  throw new Error("Missing return statement in function");
}
