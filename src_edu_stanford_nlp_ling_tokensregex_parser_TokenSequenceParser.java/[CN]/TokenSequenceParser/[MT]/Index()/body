{
  Token t;
  jj_consume_token(31);
  t=IntegerToken();
  jj_consume_token(32);
{
    if (true)     return Integer.parseInt(t.image);
  }
  throw new Error("Missing return statement in function");
}
