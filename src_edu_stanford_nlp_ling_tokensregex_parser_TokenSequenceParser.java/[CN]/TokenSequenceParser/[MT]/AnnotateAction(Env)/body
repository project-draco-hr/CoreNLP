{
  Map<String,String> attributes;
  jj_consume_token(51);
  jj_consume_token(24);
  attributes=SetAttrValues(env);
  jj_consume_token(25);
{
    if (true)     return new CoreMapSequenceMatchAction.AnnotateAction<CoreMap>(attributes);
  }
  throw new Error("Missing return statement in function");
}
