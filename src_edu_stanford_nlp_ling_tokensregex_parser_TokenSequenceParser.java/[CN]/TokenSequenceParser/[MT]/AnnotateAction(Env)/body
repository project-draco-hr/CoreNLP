{
  Map<String,String> attributes;
  jj_consume_token(54);
  jj_consume_token(25);
  attributes=SetAttrValues(env);
  jj_consume_token(26);
{
    if (true)     return new CoreMapSequenceMatchAction.AnnotateAction<>(attributes);
  }
  throw new Error("Missing return statement in function");
}
