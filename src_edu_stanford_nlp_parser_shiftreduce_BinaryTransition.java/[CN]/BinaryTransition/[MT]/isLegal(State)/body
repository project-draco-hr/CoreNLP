{
  if (state.finished) {
    return false;
  }
  if (state.stack.size() <= 1) {
    return false;
  }
  if (state.stack.size() == 2 && isBinarized() && state.endOfQueue()) {
    return false;
  }
  return true;
}
