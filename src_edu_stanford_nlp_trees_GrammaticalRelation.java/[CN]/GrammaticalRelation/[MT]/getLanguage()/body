{
switch (this.language) {
case Any:
    return Optional.empty();
case English:
  return Optional.of(Languages.Language.English);
case Chinese:
return Optional.of(Languages.Language.Chinese);
default :
throw new IllegalStateException("Unknown language: " + this.language);
}
}
