{
  ImageIcon icon=loadImageIcon(iconFilename);
  if (icon != null) {
    button.setIcon(icon);
    button.setText(null);
  }
 else   if (defaultText != null) {
    button.setText(defaultText);
  }
}
